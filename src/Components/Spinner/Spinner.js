import React from 'react';

import classes from './Spinner.css';

const Spinner = ()=>{
    //console.log(classes.Loader);
    return(
    <div className={classes.Loader}> </div>
    );
};

export default Spinner;